# Requirements for Hari_Dissertation (Python 3.10)
# Tested for AutoGluon 1.3.1 with model backends and HF transformers.
# Notes:
# - scikit-learn pinned to 1.5.2 to match trained artifacts and avoid InconsistentVersionWarning.
# - tabulate added for pandas .to_markdown().
# - Torch/Transformers pinned to avoid HF torch>=2.6 restriction while keeping fastai compatible.

autogluon.tabular==1.3.1
python-dotenv
numpy
pandas
requests

# PyTorch stack
torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1

# Transformers / HF
transformers==4.44.2
huggingface_hub==0.25.2
safetensors>=0.4.5
openai

# AutoGluon model backends
scikit-learn==1.5.2
lightgbm==4.5.0
xgboost==2.1.1
catboost==1.2.7
fastai==2.7.18

# CLI / formatting
tabulate==0.9.0
